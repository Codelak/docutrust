{
  "name": "docutrust-security-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Live secret verification tooling for DevSecOps Project 1 (kept separate so the app's dependencies stay untouched)",
  "dependencies": {
    "@aws-sdk/client-sts": "^3.600.0"
  }
}
