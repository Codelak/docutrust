node_modules
.env
.env.local
*.log
timeout-*
crash-*

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# local Claude Code config — machine-local, not committed.
.claude/
