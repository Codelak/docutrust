               
               
┌─────────────┐
│ Scan Status │
└─────────────┘
  Scanning 8 files tracked by git with 1 Code rule:
  Scanning 8 files.
                
                
┌──────────────┐
│ Scan Summary │
└──────────────┘
✅ Scan completed successfully.
 • Findings: 6 (6 blocking)
 • Rules run: 1
 • Targets scanned: 8
 • Parsed lines: ~100.0%
 • Scan was limited to files tracked by git
 • For a detailed list of skipped files and lines, run semgrep with the --verbose flag
Ran 1 rule on 8 files: 6 findings.
                   
                   
┌─────────────────┐
│ 6 Code Findings │
└─────────────────┘
                                         
    evidence/05-custom-rule/test-cases.js
   ❯❯❱ semgrep.rules.docutrust-unsafe-sql-interpolation
          ❰❰ Blocking ❱❱
          A SQL query string is built with template-literal interpolation (${...}) or string concatenation
          instead of a parameterized query. This is a SQL injection risk: user input flows into the query 
          text. Use bound parameters instead: pool.query("SELECT ... WHERE x = $1", [value]).             
                                                                                                          
            8┆ const rows = await pool.query(`SELECT * FROM users WHERE id = ${userId}`);
            ⋮┆----------------------------------------
           14┆ return pool.query("SELECT * FROM users WHERE name = '" + name + "'");
            ⋮┆----------------------------------------
           21┆ const result = await pool.query(query);
            ⋮┆----------------------------------------
           27┆ return pool.query("UPDATE documents SET body = '" + body + "' WHERE id = " + id);
            ⋮┆----------------------------------------
   ❯❯❱ semgrep.rules.docutrust-unsafe-sql-interpolation
          ❰❰ Blocking ❱❱
          A SQL query string is built with template-literal interpolation (${...}) or string concatenation
          instead of a parameterized query. This is a SQL injection risk: user input flows into the query 
          text. Use bound parameters instead: pool.query("SELECT ... WHERE x = SELECT", [value]).         
                                                                                                          
           32┆ const q = "SELECT * FROM documents WHERE id = " + id;
            ⋮┆----------------------------------------
   ❯❯❱ semgrep.rules.docutrust-unsafe-sql-interpolation
          ❰❰ Blocking ❱❱
          A SQL query string is built with template-literal interpolation (${...}) or string concatenation
          instead of a parameterized query. This is a SQL injection risk: user input flows into the query 
          text. Use bound parameters instead: pool.query("SELECT ... WHERE x = WHERE", [value]).          
                                                                                                          
           32┆ const q = "SELECT * FROM documents WHERE id = " + id;

